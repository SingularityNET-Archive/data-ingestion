import React from 'react'

export default function App() {
  return (
    <div style={{padding:20,fontFamily:'sans-serif'}}>
      <h1>Ingestion Dashboard (Prototype)</h1>
      <p>Front-end scaffold — connect to backend at <code>/api</code></p>
    </div>
  )
}
